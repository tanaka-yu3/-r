

<% if @book.errors.any? %>
   <p>2 errors prohibited this obj from being saved:
Title can't be blank
Body can't be blank</p>
<% end %>

<%= render"users/list",user:current_user ,book:@book %>

<div class='col-xs-9'>
<h1>Users</h1>
<div class="container"> <!-- ここを追加 -->
      <div class="row"> 
      <div class="col-lg-8">   <!-- ここを追加 -->
<table class="table table-striped">
	<tr><td>image</td><td>name</td><td></td></tr>
	<tr><% @users.each do |user| %>
		<td><%= attachment_image_tag user , :profile_image, :fill,45,45,format:"jpeg",fallback: "no_image.jpg" %></td>
		<td><%= user.name %></td>
		<td><%= link_to "Show",user_path(user.id)%></td>
	</tr>
	<% end %>
</table>
</div>
</div>
</div>


